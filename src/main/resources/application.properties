spring.application.name=demo
server.port=9001
server.forward-headers-strategy=framework 

# Database connectivity
spring.datasource.url=jdbc:mysql://localhost:3306/sample?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configuration
# FIX: Changed ddt-auto to ddl-auto
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show.sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# JWT Configuration
# FIX: Added keys required by JwtTokenProvider constructor
app.jwt.secret=thisIsA32ByteMinimumSecureJwtTestKey!
app.jwt.expiration=3600000

# Security Defaults
spring.security.user.name=madanraj
spring.security.user.password=krishnesh